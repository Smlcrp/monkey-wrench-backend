module.exports = app => {
    const jobs = require('../controllers/jobs.controller');

    let router = require('express').Router();

    // create a new job

    // get all jobs

    // get a job

    // update a job

    // delete a job
}